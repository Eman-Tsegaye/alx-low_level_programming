#include <stdio.h>
#include <string.h>

/**
* main - Entry point
* Return: Always 0
*/

int main(void)

{
char output[100];
strcpy(output, "\"Programming is like building a multilingual puzzle");
puts(output);
return (0);
}
